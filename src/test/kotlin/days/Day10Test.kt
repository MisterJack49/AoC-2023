package days

import org.hamcrest.MatcherAssert.assertThat
import org.hamcrest.core.Is.`is`
import org.junit.jupiter.api.Test

class Day10Test {

    @Test
    fun testPartOne() {
        assertThat(Day10().partOne(), `is`(""))
    }

    @Test
    fun testPartTwo() {
        assertThat(Day10().partTwo(), `is`(""))
    }
}
